{
  "name": "neutrona",
  "version": "0.1.0",
  "description": "End-to-end, browser-based, integration test framework",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "browserify": "mkdir -p target; browserify boot.js > target/neutrona-bundle.js",
    "watchify": "mkdir -p target; watchify --debug boot.js -v -o target/neutrona-bundle.js"
  },
  "keywords": [
    "testing",
    "integration",
    "testing",
    "browser",
    "end-to-end",
    "test",
    "automation"
  ],
  "author": "Marcel Toele",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.1.1",
    "watchify": "^3.7.0"
  },
  "dependencies": {
    "jquery": "^3.1.1",
    "node-uuid": "^1.4.7"
  }
}
